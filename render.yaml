services:
  - type: web
    name: kwchecker-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: JWT_SECRET
        generateValue: true
